[project]
name = "arbitrage-bot"
version = "1.0.0"
description = "Cross-Market Latency Arbitrage & Prediction Bot"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
